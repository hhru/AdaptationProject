events {
    worker_connections  1024;
}

http {
    server {
        listen 80;

        root /opt/frontend;

        location / {
            try_files $uri /index.html;
        }

        location /api/ {
            proxy_pass http://backend:9999/;
        }
    }
}